# koda
A lightweight RAG-based knowledge engine built to transform static PDFs into interactive datasets using semantic search and LLM synthesis. It leverages vector embeddings and precise retrieval to provide grounded, context-aware insights from documents.
